#ifndef TICTACTOE_H
#define TICTACTOE_H

#include "http&tcp/HttpResponse.h"
#include "http&tcp/HttpRequest.h"
#include "http&tcp/HttpSocket.h"
#include "http&tcp/HttpServer.h"
#include "Game.h"
#include <QObject>
#include <QFile>
#include <deque>
#include <QUrl>
#include <vector>
#include <QMap>
#include <set>
#include <algorithm>

class TicTacToe {
private:
    HttpServer httpServer;

public:
    TicTacToe();
    void start(uint16_t port);
    void stop();
};
void request(HttpRequest& http_request, HttpSocket& socket);

#endif // TICTACTOE_H
